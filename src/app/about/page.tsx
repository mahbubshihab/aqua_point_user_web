import React from 'react';
import { AboutContactView } from '@/features/about_contact/AboutContactView';

export const metadata = {
  title: 'About Aqua Point | Leading Water Treatment Provider',
  description: 'Learn about Aqua Point 7-stage RO purifiers, technology standards, and corporate history in Bangladesh.',
};

export default function AboutPage() {
  return <AboutContactView />;
}
